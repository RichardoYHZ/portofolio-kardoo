import { useEffect, useRef } from 'react'
import { Icon } from './Icon'
export function ProjectModal({ project, onClose }) { const closeRef = useRef(null); useEffect(() => { if (!project) return; const escape = (e) => e.key === 'Escape' && onClose(); document.body.classList.add('modal-open'); window.addEventListener('keydown', escape); closeRef.current?.focus(); return () => { document.body.classList.remove('modal-open'); window.removeEventListener('keydown', escape) } }, [project, onClose]); if (!project) return null; return <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><section className="modal" role="dialog" aria-modal="true" aria-labelledby="modal-title" onMouseDown={(e) => e.stopPropagation()}><button ref={closeRef} className="modal-close" onClick={onClose} aria-label="Close project details"><Icon name="close" /></button><img src={project.image} alt="" /><p className="eyebrow">{project.category}</p><h2 id="modal-title">{project.name}</h2><p>{project.description}</p><h3>Technology</h3><div className="skill-tags">{project.stack.map((item) => <span key={item}>{item}</span>)}</div></section></div> }
