import { useEffect, useState } from 'react'
import { Icon } from './Icon'
import { ThemeToggle } from './ThemeToggle'
export function Header({ navigation, active, scrolled, name }) { const [open, setOpen] = useState(false); useEffect(() => { const close = () => setOpen(false); window.addEventListener('resize', close); return () => window.removeEventListener('resize', close) }, []); return <header className={`site-header ${scrolled ? 'is-scrolled' : ''}`}><div className="nav-wrap"><a className="brand" href="#home" aria-label="Back to home"><span className="brand-mark" aria-hidden="true"><svg viewBox="0 0 32 32" fill="none"><path d="m12 8-7 8 7 8M20 8l7 8-7 8M18 5l-4 22" /></svg></span><span className="brand-name">{name}<b>.</b></span></a><button className="menu-button" aria-label={open ? 'Close menu' : 'Open menu'} aria-expanded={open} onClick={() => setOpen(!open)}><Icon name={open ? 'close' : 'menu'} /></button><nav className={open ? 'open' : ''} aria-label="Primary navigation">{navigation.map(([id, label]) => <a className={active === id ? 'active' : ''} href={`#${id}`} onClick={() => setOpen(false)} key={id}>{label}</a>)}<ThemeToggle /></nav><div className="desktop-toggle"><ThemeToggle /></div></div></header> }
