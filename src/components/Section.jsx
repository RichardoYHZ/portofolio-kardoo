import { useEffect, useRef } from 'react'
export function Section({ id, eyebrow, title, children }) { const ref = useRef(null); useEffect(() => { const node = ref.current; const observer = new IntersectionObserver(([entry]) => { if (entry.isIntersecting) { node.classList.add('is-visible'); observer.unobserve(node) } }, { threshold: .12 }); observer.observe(node); return () => observer.disconnect() }, []); return <section id={id} className="content-section section-shell reveal-section" ref={ref}><header className="section-heading"><p className="eyebrow">{eyebrow}</p><h2>{title}</h2></header>{children}</section> }
